{
  "name": "dev-hyd-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "cd dev.hyd/server && node app.js",
    "build": "cd dev.hyd/server && npm install && npx prisma generate",
    "install:all": "npm run install:all --prefix dev.hyd",
    "dev": "npm run dev --prefix dev.hyd"
  }
}
